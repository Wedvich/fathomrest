// Fathomrest simulation core — pure TS, no React/Pixi. Spine lands in the next session
// (see ADR-0001): state document, table accessors, seeded PRNG, clock, event heap,
// advance(t) / query(t). This placeholder only anchors the package export.

export const CORE_PACKAGE = "@fathomrest/core";
